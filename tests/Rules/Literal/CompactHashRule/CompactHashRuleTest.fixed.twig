{{ {foo:foo, bar:baz, baz:baz + foo, 42: hello, hello: 42, thing:thing} }}

{{ include('foo', {product:product}) }}
{{ include('foo', {
    product:product
}) }}

{{ include('foo', {product:product}) }}
{{ include('foo', {
    product:product
}) }}

{{ include('foo', {product: product}) }}
{{ include('foo', {
    product: product
}) }}

{{ include('foo', {product :product}) }}
{{ include('foo', {
    product :product
}) }}

{% types {
    is_correct: 'boolean',
    score?: 'number',
} %}
