<?xml version="1.0" encoding="UTF-8"?>

<ruleset xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:noNamespaceSchemaLocation="vendor/squizlabs/php_codesniffer/phpcs.xsd">

    <config name="installed_paths" value="../../vincentlanglet/symfony-custom-coding-standard"/>

    <arg name="basepath" value="."/>
    <arg name="cache" value=".phpcs-cache"/>
    <arg name="colors"/>
    <arg name="extensions" value="php"/>

    <rule ref="SymfonyCustom">
        <!-- Since we are using bytes as position, mb_ methods are voluntary not used. -->
        <exclude name="SymfonyCustom.PHP.EncourageMultiBytes" />
    </rule>

    <file>src/</file>
    <file>tests/</file>
</ruleset>
